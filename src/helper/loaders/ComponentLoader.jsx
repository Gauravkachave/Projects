import * as React from 'react';
import LinearProgress from '@material-ui/core/LinearProgress';

const loading = () => (
    <LinearProgress className='loader__linear-progress' color="secondary" />
);
export default loading;
